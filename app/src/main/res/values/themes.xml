<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyRecyclerView" parent="Theme.Material3.Dark">
        <!-- Customize your light theme here. -->
        <item name="android:backgroundTint">@color/purpleblack</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/purpleblack</item>
        <item name="colorOnPrimary">@color/purpleblack</item>
        <item name="colorPrimaryVariant">@color/purpleblack</item>
        <item name="android:statusBarColor">@color/purpleblack</item>

    </style>

    <style name="Theme.MyRecyclerView" parent="Base.Theme.MyRecyclerView" />
    
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/purpleblack</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/sz</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.MyRecyclerView</item>
    </style>
</resources>